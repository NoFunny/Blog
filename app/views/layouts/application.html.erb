<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <!-- CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

    <!-- jQuery and JS bundle w/ Popper.js -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ho+j7jyWK8fNQe+A12Hb8AhRq26LrZ/JpcUGGOn+Y7RsweNrtN/tE3MoK7ZeZDyx" crossorigin="anonymous"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
     <div class="bg-dark text-light container" style="border-radius: 0% 0% 18% 18%">
      <nav class="bg-dark text-light navbar navbar-light bg-light" style="border-radius: 0% 0% 50% 50%" >
        <ul class="nav navbar-nav">
          <li class="nav-item active">
            <a class="btn btn-dark text-light nav-link" href="<%= posts_path %>">SUPER_BLOG</a>
          </li>
          <% if can? :manage, Post %>
          <li class="nav-item">
            <a class="text-light nav-link btn btn-dark" href="<%= new_post_path %>">Новая статья</a>
          </li>
          <% end %>
        </ul>
        <ul class="navbar-nav pull-xs-right">
             <% if user_signed_in? %>
               <li class="nav-item">
                 <% if can? :manage, Post %>
                   Администратор:
                 <% else %>
                   Пользователь:
                 <% end %>
                 <%= current_user.email %>
               </li>
               <li class="nav-item">
                 <a><%= link_to "Выход", destroy_user_session_path, method: :delete, class: "btn btn-dark text-light nav-link" %></a>
               </li>
             <% else %>
               <li class="nav-item">
                 <a><%= link_to "Вход", new_user_session_path, class: "btn btn-dark text-light nav-link" %></a>
               </li>
               <li class="nav-item">
                 <a><%= link_to "Регистрация", new_user_registration_path, class: "btn btn-dark text-light nav-link" %></a>
               </li>
             <% end %>
           </ul>
      </nav>
    </div>

    <div class="container main">
      <div class="row">
        <div class ="col-md-12">
          <% flash.each do |name, message| %>
          <div class="alert <%= bootstrap_class_for(name) %>">
              <button class="close" data-dismiss="alert">&times;</button>
            <%= message %>
          </div>
          <% end %>
        </div>
      </div>

      <%= yield %>
    </div>
  </body>
</html>
